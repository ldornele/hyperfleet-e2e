adapterConfig:
  create: true
  files:
    adapter-config.yaml: cl-deployment/adapter-config.yaml
  log:
    level: debug

adapterTaskConfig:
  create: true
  files:
    task-config.yaml: cl-deployment/adapter-task-config.yaml
    deployment.yaml: cl-deployment/adapter-task-resource-deployment.yaml

broker:
  type: googlepubsub
  create: true
  googlepubsub:
    projectId: CHANGE_ME
    subscriptionId: CHANGE_ME
    topic: CHANGE_ME
    deadLetterTopic: CHANGE_ME

image:
  registry: CHANGE_ME
  repository: ci/hyperfleet-adapter
  pullPolicy: Always
  tag: latest

rbac:
   resources:
     - deployments
     - deployments/status
